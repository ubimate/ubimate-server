services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.unified
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3001}
        VITE_HOCUSPOCUS_URL: ${VITE_HOCUSPOCUS_URL:-ws://localhost:3001/yjs}
    env_file:
      - path: .env
        required: false
    environment:
      NODE_ENV: production
    volumes:
      - app-data:/app/apps/api/data
    ports:
      - "3001:3001"
    restart: unless-stopped

volumes:
  app-data:
